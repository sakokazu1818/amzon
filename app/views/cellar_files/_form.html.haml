= form_for @cellar_file, html: { class: "form form-horizontal cellar_file" } do |f|
  = f.hidden_field :name, value: @cellar_file.name
  .form-group
    = f.label :excel, class: 'control-label col-lg-2'
    .col-lg-10
      = f.file_field :excel, class: 'form-control', required: true
    = f.error_span(:name)
  .form-group
    .col-lg-offset-2.col-lg-10
      = f.submit nil,class: 'btn btn-primary'
      = link_to t('.cancel', :default => t("helpers.links.cancel")), cellar_files_path, :class => 'btn btn-default'