wb = xlsx_package.workbook
wb.styles do |style|
  wb.add_worksheet do |sheet|
    @xlsx_arr.each do |cellar_arr|
      cellar_arr.each do |key, cellar|
        sheet.add_row [cellar], style: [nil]
      end
    end
  end
end